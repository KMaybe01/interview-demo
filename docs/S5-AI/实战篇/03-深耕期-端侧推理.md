# 🟣 阶段三：深耕期 - 端侧推理

> 📖 **本文档为《AI 前端开发体系化学习指南》的阶段拆分文档**
> 完整指南请查看：[学习指南总览](./README.md#-ai-前端开发体系化学习指南)

---

> 🎯 **阶段目标**：突破云端限制，在浏览器端实现隐私保护、零延迟的 AI 推理。

### 💡 你将学到
- 浏览器端机器学习原理（[WebAssembly](https://webassembly.org) / [WebGPU](https://www.w3.org/TR/webgpu/) / WebGL）
- Transformers.js 的 Pipeline 机制与模型懒加载
- [ONNX](https://onnxruntime.ai) 格式量化模型的加载与运行
- [WebGPU](https://www.w3.org/TR/webgpu/) 硬件加速推理与 GPU 适配降级
- IndexedDB 模型缓存与离线可用性实现

### 🔗 前置知识
- 完成 [🔵 阶段二：进阶期](./02-进阶期-RAG应用.md)
- 了解浏览器性能 API（performance, navigator）
- 熟悉 JavaScript 异步编程与 ArrayBuffer

### 📚 核心能力指标
- [ ] 理解浏览器端机器学习原理 ([WebAssembly](https://webassembly.org) / [WebGPU](https://www.w3.org/TR/webgpu/))
- [ ] 掌握 Transformers.js 核心 API 与 Pipeline 机制
- [ ] 能够加载并运行 [ONNX](https://onnxruntime.ai) 格式的量化模型
- [ ] 实现 [WebGPU](https://www.w3.org/TR/webgpu/) 加速推理，优化内存占用
- [ ] 管理模型缓存 (IndexedDB) 与离线可用性功能

### 🧠 核心概念解析

#### 3.1 端侧推理架构

```mermaid
graph TD
    A[浏览器环境] --> B{硬件加速选择}
    B -->|高性能| C[WebGPU]
    B -->|兼容性| D[WebGL]
    B -->|兜底| E[WebAssembly]

    C --> F[ONNX Runtime Web]
    D --> F
    E --> F

    F --> G[Transformers.js]
    G --> H[模型推理]
    H --> I[文本/图像输出]

    classDef browser fill:#fff3e0,stroke:#e65100,stroke-width:2px;
    class A,B,C,D,E,F,G,H,I browser;
```

#### 3.2 技术栈对比

| 技术 | 优势 | 劣势 | 适用场景 |
|:---|:---|:---|:---|
| **WebGPU** | 极致性能，支持通用计算 | 兼容性较新 (Chrome 113+) | 大规模矩阵运算、大模型推理 |
| **WebGL** | 广泛兼容，图形渲染强 | API 繁琐，非通用计算设计 | 轻量级模型、可视化 |
| **WebAssembly** | 接近原生 CPU 性能 | 无法利用 GPU 并行 | 传统 ML 算法、兜底方案 |

#### 3.3 KV Cache —— 推理"加速器"

**问题：** 生成每个新词都要重新计算所有历史词的Attention。生成N个词，总计算量O(N³/3)。有KV Cache后**仅在解码阶段**降为O(N²/2)，N=4096时效率提升约2000倍（仅指每一步 decode，不含 prefill）。

**原理：** 把已算过的Key/Value缓存起来，每次只算新词的K/V，历史K/V直接复用。

```
生成Token 1: 计算K1,V1 → 缓存
生成Token 2: 读K1,V1 + 算K2,V2 → 追加缓存
生成Token 3: 读K1,K2,V1,V2 + 算K3,V3 ...
每次节省80%+的重复计算
```

> ⚠️ **重要说明**：上述"O(N²/2)"严格意义是**单步 decode 的时间复杂度**（避免了对 K/V 的完整重算），不是整个推理序列的总复杂度。Prefill 阶段仍需 O(N²) 处理完整输入。N=4096 时的 2000 倍加速，是相对"无缓存的 decode 步骤"的近似估算（≈ N/2）。

**KV Cache 内存公式：**
```
M_KV = 2 × L × H_kv × S × P × B
L=层数, H_kv=KV头数（GQA下小于Q头数）, S=序列长度, P=精度(FP16=2), B=Batch Size
```

**实际对比：** Llama-70B用MHA时（64 KV heads）单序列 KV Cache ≈ 10.7 GB；用 GQA（8 KV heads）后降至 ≈ 1.34 GB——节省 8 倍。这就是GQA的核心价值——大幅减少KV Cache。

**代价与挑战：** KV Cache 显存为 O(L·S·d·2)，长上下文场景需结合 **PagedAttention**（vLLM 思路）、**GQA**、**KV Cache 量化**（INT8/INT4）来优化。

**Prompt Caching：** 多个请求共享相同System Prompt时，缓存其KV向量直接复用。vLLM用哈希表实现（适合固定前缀），SGLang用RadixAttention基数树实现（更灵活，支持中间共享），后者缓存命中率更高。

---

### 🛠️ 环境搭建

#### 3.3 项目初始化

```bash
# 🚀 创建项目
npx create-next-app@latest edge-ai --typescript --tailwind --app
cd edge-ai

# 📦 安装 Transformers.js
npm install @huggingface/transformers
```

### 💻 核心实现

#### 3.4 Transformers.js 核心 Pipeline

```typescript
// lib/transformers-pipeline.ts
import { pipeline, env, Pipeline } from '@huggingface/transformers';

// ⚙️ 全局配置
env.allowLocalModels = false;
env.useBrowserCache = true;

export class TransformersPipeline {
  private pipelines: Map<string, Pipeline> = new Map();

  // 🚀 懒加载 Pipeline
  async getPipeline(task: string, model?: string): Promise<Pipeline> {
    const key = `${task}:${model || 'default'}`;
    if (this.pipelines.has(key)) return this.pipelines.get(key)!;

    const defaultModels: Record<string, string> = {
      'text-generation': 'onnx-community/Qwen2.5-0.5B-Instruct',
      'text-classification': 'Xenova/distilbert-base-uncased-finetuned-sst-2-english',
      'feature-extraction': 'Xenova/all-MiniLM-L6-v2',
    };

    const p = await pipeline(task, model || defaultModels[task], { dtype: 'q4' }); // 4-bit 量化
    this.pipelines.set(key, p);
    return p;
  }

  // 💬 文本生成
  async generateText(prompt: string, maxTokens = 256): Promise<string> {
    const generator = await this.getPipeline('text-generation');
    const res = await generator(prompt, { max_new_tokens: maxTokens, temperature: 0.7 });
    return res[0].generated_text;
  }

  // 📊 文本分类 (情感分析)
  async classifyText(text: string): Promise<Array<{ label: string; score: number }>> {
    const classifier = await this.getPipeline('text-classification');
    return await classifier(text);
  }

  // 🔢 文本向量化
  async embedText(text: string): Promise<Float32Array> {
    const extractor = await this.getPipeline('feature-extraction');
    const res = await extractor(text, { pooling: 'mean', normalize: true });
    return res.data as Float32Array;
  }
}
```

#### 3.5 [WebGPU](https://www.w3.org/TR/webgpu/) 加速引擎

```typescript
// lib/webgpu-engine.ts
export class WebGPUEngine {
  private device: GPUDevice | null = null;

  async initialize(): Promise<boolean> {
    if (!('gpu' in navigator)) return false;
    try {
      const adapter = await navigator.gpu.requestAdapter();
      if (!adapter) return false;
      this.device = await adapter.requestDevice();
      console.log('✅ WebGPU 初始化成功');
      return true;
    } catch (e) {
      console.warn('❌ WebGPU 不可用，回退到 WASM');
      return false;
    }
  }

  // 🧮 矩阵乘法 (GPU 并行计算示例)
  async matrixMultiply(a: Float32Array, b: Float32Array): Promise<Float32Array> {
    if (!this.device) throw new Error('WebGPU 未初始化');
    // ... 省略具体 Shader 编写与 Buffer 绑定逻辑 ...
    // 实际开发中通常直接使用 Transformers.js 的 WebGPU 后端
    return new Float32Array();
  }
}
```

---

### ⚡ [WebGPU](https://www.w3.org/TR/webgpu/) Compute Shader 编程详解

> **深入 GPU 编程**：理解 Compute Shader 是优化端侧推理性能的关键，本节用矩阵乘法示例讲解完整的 [WebGPU](https://www.w3.org/TR/webgpu/) 计算管线。

#### [WebGPU](https://www.w3.org/TR/webgpu/) 计算管线架构

```mermaid
graph TD
    JS["JavaScript 侧"] --> PipelineStep1["1. 请求适配器<br/>navigator.gpu.requestAdapter()"]
    PipelineStep1 --> PipelineStep2["2. 创建设备<br/>adapter.requestDevice()"]
    PipelineStep2 --> PipelineStep3["3. 创建 Buffer<br/>上传数据到 GPU"]
    PipelineStep3 --> PipelineStep4["4. 编写 Shader<br/>WGSL 计算着色器"]
    PipelineStep4 --> PipelineStep5["5. 创建管线<br/>ComputePipeline"]
    PipelineStep5 --> PipelineStep6["6. 绑定资源<br/>BindGroup"]
    PipelineStep6 --> PipelineStep7["7. 派发计算<br/>pass.dispatchWorkgroups()"]
    PipelineStep7 --> PipelineStep8["8. 读取结果<br/>GPU → CPU 回读"]
    
    classDef gpu fill:#e8eaf6,stroke:#283593;
    class JS,PipelineStep1,PipelineStep2,PipelineStep3,PipelineStep4,PipelineStep5,PipelineStep6,PipelineStep7,PipelineStep8 gpu;
```

#### WGSL ([WebGPU](https://www.w3.org/TR/webgpu/) Shading Language) 基础

```wgsl
// Matrix Multiply Compute Shader (WGSL)
@group(0) @binding(0) var<storage, read> a: array<f32>;  // 输入矩阵 A
@group(0) @binding(1) var<storage, read> b: array<f32>;  // 输入矩阵 B
@group(0) @binding(2) var<storage, read_write> c: array<f32>; // 输出矩阵 C

const BLOCK_SIZE: u32 = 16u;

@compute @workgroup_size(BLOCK_SIZE, BLOCK_SIZE)
fn main(@builtin(global_invocation_id) gid: vec3<u32>) {
    let row = gid.y;
    let col = gid.x;
    let k = u32(128u); // 矩阵维度
    
    var sum = 0.0;
    for (var i = 0u; i < k; i = i + 1u) {
        sum = sum + a[row * k + i] * b[i * k + col];
    }
    c[row * k + col] = sum;
}
```

| WGSL 概念 | 类比 JavaScript | 用途 |
|:---|:---|:---|
| `@group(N) @binding(M)` | 函数参数声明 | 绑定 GPU 资源（Buffer/Texture） |
| `@compute` | async function | 标记为计算着色器入口 |
| `@workgroup_size(X, Y, Z)` | Promise.all 并行度 | 每个工作组包含的线程数 |
| `var<storage, read>` | const 参数 | 只读存储缓冲区 |
| `global_invocation_id` | 循环索引 | 当前线程的全局 ID |

#### 完整 [WebGPU](https://www.w3.org/TR/webgpu/) 计算管线

```typescript
// webgpu-compute.ts - 完整计算管线
export class WebGPUComputeEngine {
  private device: GPUDevice | null = null;
  private queue: GPUQueue | null = null;

  async init(): Promise<void> {
    const adapter = await navigator.gpu.requestAdapter({
      powerPreference: 'high-performance',
    });
    if (!adapter) throw new Error('No WebGPU adapter found');
    
    this.device = await adapter.requestDevice();
    this.queue = this.device.queue;
  }

  // 矩阵乘法完整实现
  async matmul(
    size: number,
    matA: Float32Array,
    matB: Float32Array,
  ): Promise<Float32Array> {
    if (!this.device || !this.queue) throw new Error('Not initialized');

    // 1. 创建 GPU Buffer
    const bufferA = this.device.createBuffer({
      size: matA.byteLength,
      usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST,
    });
    const bufferB = this.device.createBuffer({
      size: matB.byteLength,
      usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_DST,
    });
    const bufferC = this.device.createBuffer({
      size: matA.byteLength, // 同维度方阵
      usage: GPUBufferUsage.STORAGE | GPUBufferUsage.COPY_SRC,
    });
    const readBuffer = this.device.createBuffer({
      size: matA.byteLength,
      usage: GPUBufferUsage.MAP_READ | GPUBufferUsage.COPY_DST,
    });

    // 2. 上传数据到 GPU
    this.queue.writeBuffer(bufferA, 0, matA);
    this.queue.writeBuffer(bufferB, 0, matB);

    // 3. 编译 Shader
    const shaderModule = this.device.createShaderModule({
      code: MATMUL_SHADER_CODE, // 上面的 WGSL 代码
    });

    // 4. 创建计算管线
    const pipeline = this.device.createComputePipeline({
      layout: 'auto',
      compute: { module: shaderModule, entryPoint: 'main' },
    });

    // 5. 绑定资源
    const bindGroup = this.device.createBindGroup({
      layout: pipeline.getBindGroupLayout(0),
      entries: [
        { binding: 0, resource: { buffer: bufferA } },
        { binding: 1, resource: { buffer: bufferB } },
        { binding: 2, resource: { buffer: bufferC } },
      ],
    });

    // 6. 编码并提交命令
    const encoder = this.device.createCommandEncoder();
    const pass = encoder.beginComputePass();
    pass.setPipeline(pipeline);
    pass.setBindGroup(0, bindGroup);
    pass.dispatchWorkgroups(Math.ceil(size / 16), Math.ceil(size / 16));
    pass.end();

    // 7. 复制结果到可读 Buffer
    encoder.copyBufferToBuffer(bufferC, 0, readBuffer, 0, matA.byteLength);
    this.queue.submit([encoder.finish()]);

    // 8. 回读结果
    await readBuffer.mapAsync(GPUMapMode.READ);
    const result = new Float32Array(readBuffer.getMappedRange());
    const output = new Float32Array(result);
    readBuffer.unmap();

    // 清理
    bufferA.destroy(); bufferB.destroy(); bufferC.destroy(); readBuffer.destroy();

    return output;
  }
}
```

#### [WebGPU](https://www.w3.org/TR/webgpu/) 内存优化策略

| 技术 | 说明 | 效果 |
|:---|:---|:---|
| **Buffer 复用池** | 预分配固定大小 Buffer，避免重复创建/销毁 | 减少 40% 内存分配开销 |
| **Ping-Pong Buffer** | 双 Buffer 交替使用，流水线数据读写 | 提高 GPU 利用率 |
| **子区域映射** | 只映射需要的 Buffer 片段，而非全量 | 减少 CPU-GPU 拷贝 |
| **Texture 替代 Buffer** | 利用 Texture 缓存局部性更优的特点 | 矩阵运算提速 20% |

```typescript
// Buffer 复用池实现
class GPUBufferPool {
  private pool: Map<number, GPUBuffer[]> = new Map();
  
  acquire(device: GPUDevice, size: number, usage: number): GPUBuffer {
    const key = `${size}:${usage}`;
    const buffers = this.pool.get(key) || [];
    if (buffers.length > 0) return buffers.pop()!;
    return device.createBuffer({ size, usage });
  }
  
  release(buffer: GPUBuffer): void {
    const key = `${buffer.size}:${buffer.usage}`;
    if (!this.pool.has(key)) this.pool.set(key, []);
    this.pool.get(key)!.push(buffer);
  }
  
  destroyAll(): void {
    for (const buffers of this.pool.values()) {
      for (const buffer of buffers) buffer.destroy();
    }
    this.pool.clear();
  }
}
```

---

### 🔢 模型量化技术详解

> **量化是端侧推理的核心**：将 FP32 模型压缩为 INT4/INT8，使 7B 模型从 28GB 缩减至 4GB。

#### 量化精度对比

| 精度 | 位数 | 模型大小 (7B) | 质量损失 | 速度提升 |
|:---:|:---:|:---:|:---:|:---:|
| FP32 | 32bit | 28GB | 基准 | 1x |
| FP16 | 16bit | 14GB | 几乎无损 | 1.5x |
| INT8 | 8bit | 7GB | 轻微 | 2x |
| INT4 | 4bit | 3.5GB | 可接受 | 3x |
| NF4 | 4bit | 3.5GB | 优于 INT4 | 3x |
| INT2 | 2bit | 1.75GB | 显著下降 | 4x |

#### 量化原理（以 INT8 为例）

```mermaid
graph LR
    FP32["FP32: [−128.5, 0.3, 42.7, ...]<br/>32 位浮点表示"] --> Scale["计算缩放因子<br/>scale = max( abs(fp32) ) / 127"]
    Scale --> Quantize["量化计算<br/>int8 = round(fp32 / scale)"]
    Quantize --> INT8["INT8: [−128, 0, 43, ...]<br/>8 位整数存储"]
    INT8 --> Dequantize["反量化计算<br/>fp32 = int8 × scale"]
    Dequantize --> Approx["近似值: [−128.5, 0, 42.8, ...]"]
    
    classDef fp32 fill:#e3f2fd,stroke:#1565c0;
    classDef int8 fill:#fff3e0,stroke:#e65100;
    class FP32,Approx fp32;
    class INT8 int8;
```

#### 常用量化方法

| 方法 | 原理 | 质量表现 | 实现复杂度 |
|:---|:---|:---:|:---:|
| **RTN** (Round To Nearest) | 四舍五入到最近整数 | ⭐⭐⭐ | 🟢 低 |
| **GPTQ** | 基于 Hessian 矩阵的逐层量化 | ⭐⭐⭐⭐ | 🟡 中 |
| **AWQ** | 保留重要权重的精度（Llama 常用） | ⭐⭐⭐⭐⭐ | 🔴 高 |
| **GGUF** | Llama.cpp 的混合精度格式 | ⭐⭐⭐⭐ | 🟡 中 |

#### Transformers.js 中的量化模型加载

```typescript
// 量化模型加载与使用（最新 API）
import { pipeline, env } from '@huggingface/transformers';

// 配置优先使用量化模型
env.allowLocalModels = false; // 推荐使用远程模型
env.useBrowserCache = true;

// 使用 INT4 量化模型（自动下载）
const classifier = await pipeline(
  'text-classification',
  'Xenova/distilbert-base-uncased-finetuned-sst-2-english',
  { dtype: 'q4' } // 使用 dtype 替代已弃用的 quantized 参数
);

// 自定义量化级别
const generator = await pipeline('text-generation', 'onnx-community/Qwen2.5-0.5B-Instruct', {
  dtype: 'q4', // 'fp32' | 'fp16' | 'q8' | 'q4'
  device: 'webgpu', // 优先使用 WebGPU 运行
});

// 运行推理
const result = await generator('告诉我北京有哪些景点', {
  max_new_tokens: 128,
  temperature: 0.7,
});
```

---

### 🧪 WebAssembly (WASM) 推理引擎

当 WebGPU 不可用或模型不适合 GPU 并行计算时，WASM 是端侧推理的兜底方案。

```typescript
// 使用 ONNX Runtime Web 的 WASM 后端
import * as ort from 'onnxruntime-web';

// 配置 WASM 后端
ort.env.wasm.wasmPaths = 'https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/';
const session = await ort.InferenceSession.create('./model.onnx', {
  executionProviders: ['wasm'],
});

// WASM 多线程配置
ort.env.wasm.numThreads = navigator.hardwareConcurrency || 4;

// 运行推理
const feeds = { input: new ort.Tensor('float32', inputData, [1, seqLen]) };
const results = await session.run(feeds);
```

#### WebGPU vs WASM 性能基准

| 模型 | 参数量 | WebGPU (tok/s) | WASM (tok/s) | WASM SIMD (tok/s) |
|:---|:---:|:---:|:---:|:---:|
| Qwen2.5-0.5B | 0.5B | 45.2 | 8.7 | 12.3 |
| Qwen2.5-1.5B | 1.5B | 22.8 | 3.1 | 4.8 |
| Llama-3.2-1B | 1B | 28.5 | 4.2 | 6.7 |
| Phi-3-mini | 3.8B | 8.3 | 1.1 | 1.8 |

> **结论**：WebGPU 在模型推理上比 WASM 快 4-7 倍，但 WASM 兼容性更好（支持所有浏览器）。建议策略：**检测 WebGPU 可用 → 优先 WebGPU → 降级 WASM SIMD → 最终 WASM 单线程**。

#### Transformers.js 的 WASM 后端

```typescript
import { pipeline, env } from '@huggingface/transformers';

// 强制使用 WASM 后端（用于不支持 WebGPU 的环境）
env.backends.onnx ??= {};
env.backends.onnx.executionProviders = ['wasm'];
env.backends.onnx.wasm ??= {};
env.backends.onnx.wasm.numThreads = navigator.hardwareConcurrency;
env.backends.onnx.wasm.simd = true;

// 自动降级策略
async function createPipeline(task: string, model: string) {
  try {
    return await pipeline(task, model, { device: 'webgpu' });
  } catch {
    console.warn('WebGPU 不可用，降级至 WASM');
    env.backends.onnx ??= {}; env.backends.onnx.executionProviders = ['wasm'];
    return await pipeline(task, model);
  }
}
```

---

### ❓ 常见问题与自测

#### Q1: WebGPU、WebGL、WebAssembly 三种浏览器端推理方案如何选型？各自的兼容性如何？

**考察点**: 端侧推理技术选型

> **回答要点**:
> 1. **WebGPU**:
>    - 优势: 现代 GPU 计算 API，性能最高，支持 Compute Shader
>    - 兼容性: Chrome 113+、Edge 113+，Firefox/Safari 实验中
>    - 适用: 需要 GPU 加速的推理任务
> 2. **WebGL**:
>    - 优势: 兼容性好，几乎所有浏览器支持
>    - 劣势: 不适合通用计算，性能不如 WebGPU
>    - 适用: 简单的图像处理、轻量级推理
> 3. **WebAssembly**:
>    - 优势: 接近原生性能，跨平台，支持 SIMD
>    - 劣势: CPU 计算，不适合 GPU 加速任务
>    - 适用: 无 GPU 设备、需要 CPU 推理的场景
> 4. **选型建议**: 优先 WebGPU → 降级 WASM → 最后 WebGL
> 5. **兼容性处理**: 使用特性检测，实现自动降级策略
>
> 💡 **关键要点**: WebGPU 性能最优但兼容性有限，WASM 是最佳降级方案

#### Q2: INT4 和 INT8 量化在模型大小和推理质量上有什么权衡？如何选择合适的量化精度？

**考察点**: 模型量化技术

> **回答要点**:
> 1. **INT8 量化**:
>    - 模型大小: 原始大小的 50%（FP32→INT8）
>    - 质量损失: 通常 < 1%，几乎无感知
>    - 适用: 大多数场景，质量优先
> 2. **INT4 量化**:
>    - 模型大小: 原始大小的 25%（FP32→INT4）
>    - 质量损失: 1-3%，复杂任务可能明显
>    - 适用: 移动端、存储受限场景
> 3. **选择策略**:
>    - 对话/文本生成: INT4 通常可接受
>    - 代码生成/数学推理: 建议 INT8
>    - 图像理解: 建议 INT8 或更高
> 4. **测试方法**: 在目标任务上对比 INT4 和 INT8 的输出质量
> 5. **实际经验**: Q4_K_M 是较好的平衡点，Q5_K_M 质量更好但体积稍大
>
> 💡 **关键要点**: INT4 省空间但有质量损失，INT8 质量好但体积大

#### Q3: KV Cache 如何减少推理时的重复计算？GQA 相比 MHA 在 KV Cache 节省上有什么优势？

**考察点**: KV Cache 原理

> **回答要点**:
> 1. **KV Cache 原理**: 缓存已计算的 Key 和 Value 矩阵，避免在生成每个新 token 时重新计算
> 2. **节省效果**: 从 O(n²) 降低到 O(n)，n 为序列长度
> 3. **MHA (Multi-Head Attention)**:
>    - 每个 Head 有独立的 K、V 矩阵
>    - KV Cache 大小 = 2 × layers × heads × seq_len × head_dim
> 4. **GQA (Grouped-Query Attention)**:
>    - 多个 Head 共享一组 K、V
>    - KV Cache 大小 = 2 × layers × kv_heads × seq_len × head_dim
> 5. **节省比例**: 例如 Llama 2 70B 使用 GQA（8 个 KV Head vs 64 个 Head），KV Cache 节省 8 倍
>
> 💡 **关键要点**: KV Cache 避免重复计算，GQA 通过共享 KV 进一步节省内存

#### Q4: IndexedDB 缓存模型文件时，如何处理模型版本更新和存储空间管理？

**考察点**: 模型缓存策略

> **回答要点**:
> 1. **版本管理**: 在数据库中存储模型版本号，下载时检查版本，不一致则更新
> 2. **存储空间管理**:
>    - 使用 `navigator.storage.estimate()` 检查可用空间
>    - 设置缓存上限（如 500MB），超过时删除旧版本
>    - 实现 LRU 淘汰策略
> 3. **更新策略**:
>    - 应用启动时后台检查模型更新
>    - 使用 Service Worker 拦截请求，返回缓存或网络
>    - 支持增量更新（仅下载变化的部分）
> 4. **错误处理**: 存储满时降级到网络加载，不阻塞应用
> 5. **用户体验**: 显示缓存状态，提供手动清除缓存选项
>
> 💡 **关键要点**: 版本号管理 + LRU 淘汰 + 优雅降级

#### Q5: WebGPU 不可用时的降级策略应该如何设计？WASM SIMD 相比普通 WASM 有多大性能提升？

**考察点**: 降级策略设计

> **回答要点**:
> 1. **降级策略设计**:
>    - 特性检测: 检查 `navigator.gpu` 是否存在
>    - 性能测试: 运行小规模推理测试实际性能
>    - 自动降级: WebGPU → WASM SIMD → WASM → 网络 API
> 2. **WASM SIMD 性能提升**:
>    - 向量化计算: 一次处理多个数据点
>    - 典型提升: 2-4 倍（取决于任务类型）
>    - 矩阵运算: 提升最明显，可达 4-8 倍
> 3. **实际测试数据**:
>    - ResNet-50 推理: WASM 约 50ms，WASM SIMD 约 20ms
>    - 文本生成: WASM SIMD 比普通 WASM 快 2-3 倍
> 4. **兼容性**: WASM SIMD 在主流浏览器中支持良好（Chrome 91+、Firefox 89+）
> 5. **最佳实践**: 提供配置选项，允许用户手动选择推理后端
>
> 💡 **关键要点**: 多层降级策略 + SIMD 提升 2-4 倍 + 特性检测优先

---

### 🏆 阶段三实战项目

| 项目 | 难度 | 核心考察点 | 完成标准 |
|:---|:---:|:---|:---|
| 🟢 **离线文本分类** | ⭐⭐ | 模型加载、情感分析 | 断网可用，响应 < 100ms |
| 🔵 **端侧文本生成** | ⭐⭐⭐ | Qwen2.5-0.5B 运行、流式生成 | 浏览器内流畅对话 |
| 🟣 **图像理解应用** | ⭐⭐⭐⭐ | 图像分类、目标检测 | 实时摄像头检测 |

---

### 📎 延伸阅读

| 文档 | 内容 | 相关章节 |
|:---|:---|:---|
| [📊 技术选型对比合集](./07-技术选型对比合集.md) | WebGPU/ONNX/Transformers.js 横向对比 | 边缘计算方案、性能基准 |
| [🛠️ 开发实战与架构指南](./08-开发实战与架构指南.md) | 前端性能优化 Checklist、WebGPU 加速 | 第6章：性能优化终极 Checklist |
